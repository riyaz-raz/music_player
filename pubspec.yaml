name: music_player
description: "Offline music player with background playback"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  on_audio_query: ^2.9.0
  just_audio: ^0.9.40
  audio_service: ^0.18.15
  shared_preferences: ^2.3.3
  provider: ^6.1.2
  path_provider: ^2.1.4
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
